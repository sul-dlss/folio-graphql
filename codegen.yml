overwrite: true
schema: "schema.graphql"
documents: null
generates:
  src/schema.ts:
    plugins:
      - "typescript"
  #     - "typescript-resolvers"
  # ./graphql.schema.json:
  #   plugins:
  #     - "introspection"
